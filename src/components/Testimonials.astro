---
import { site } from "../data/site.config";
---

<section id="reviews" class="mx-auto max-w-6xl px-4 py-16">
  <div class="flex flex-col gap-3 md:flex-row md:items-end md:justify-between">
    <div>
      <h2 class="text-3xl font-bold tracking-tight">What customers say</h2>
      <p class="mt-2 text-gray-600">
        Real words from happy clients. (Swap these with your first real reviews.)
      </p>
    </div>

    <a class="rounded-lg border px-4 py-2 text-sm font-semibold hover:bg-gray-50" href="#quote">
      Leave a message
    </a>
  </div>

  <div class="mt-10 grid gap-6 md:grid-cols-2">
    {site.testimonials.map((t) => (
      <figure class="rounded-2xl border p-6">
        <blockquote class="text-gray-700">
          “{t.quote}”
        </blockquote>
        <figcaption class="mt-4 text-sm font-semibold text-gray-900">
          — {t.name}
        </figcaption>
      </figure>
    ))}
  </div>

  <div class="mt-10 rounded-2xl border bg-gray-50 p-6">
    <p class="text-sm font-semibold text-gray-700">Pro tip</p>
    <p class="mt-1 text-sm text-gray-600">
      Add review badges later (Google, Yelp) once you’re collecting reviews consistently.
    </p>
  </div>
</section>
